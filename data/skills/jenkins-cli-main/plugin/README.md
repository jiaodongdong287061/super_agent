# jk companion plugin (jk-api)

This directory will house the Jenkins companion plugin that exposes the
additional REST/SSE endpoints consumed by the CLI. Implementation begins in
Phase 3 per `docs/spec.md`.
